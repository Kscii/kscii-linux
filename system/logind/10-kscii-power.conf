[Login]
# Lid close should always suspend, including AC power and docked setups.
HandleLidSwitch=suspend
HandleLidSwitchExternalPower=suspend
HandleLidSwitchDocked=suspend

# Keep logind idle policy disabled. niri/swayidle owns idle lock/suspend timing.
IdleAction=ignore

