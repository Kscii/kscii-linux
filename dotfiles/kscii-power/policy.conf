# ThinkPad Engineering Edition — power and idle policy
#
# Values are seconds. Change these numbers, then restart swayidle or re-login.
# Set AC_SUSPEND_AFTER_SECONDS=0 to never auto-suspend while plugged in.

LOCK_AFTER_SECONDS=600
BATTERY_SUSPEND_AFTER_SECONDS=1200
AC_SUSPEND_AFTER_SECONDS=0

